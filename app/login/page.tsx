'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase/client';
export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [mensaje, setMensaje] = useState('Ingresá para continuar.'); async function onSubmit(event: FormEvent) { event.preventDefault(); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) return setMensaje('Email o contraseña incorrectos.'); setMensaje('Sesión iniciada correctamente.'); router.replace('/'); } return <section className="mx-auto max-w-md rounded-2xl border border-slate-200 bg-white p-6"><h1 className="text-2xl font-semibold">SpendWise</h1><p className="mt-1 text-sm text-slate-600">Control familiar de gastos, tarjetas y comprobantes.</p><p className="mt-4 text-sm text-slate-700">{mensaje}</p><form onSubmit={onSubmit} className="mt-4 space-y-3"><input className="w-full rounded-lg border p-2" placeholder="Email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /><input className="w-full rounded-lg border p-2" placeholder="Contraseña" type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /><button className="w-full rounded-lg bg-emerald-600 px-4 py-2 font-medium text-white">Iniciar sesión</button></form><Link href="/registro" className="mt-3 inline-block text-sm text-emerald-700">Crear cuenta</Link></section>; }
